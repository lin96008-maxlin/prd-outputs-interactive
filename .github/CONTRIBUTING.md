# 参与贡献

欢迎提交使用场景、兼容性问题、问卷交互改进和 PRD 内容质量规则。

提交前请确认：

1. 没有包含公司专属名称、真实客户数据、内部路径、账号、密钥或 Token；
2. 修改后的 Plugin 仍可以离线运行；
3. 已运行 `npm test --prefix plugins/prd-outputs-interactive`；
4. 新增规则来自可说明的真实问题，不把单个案例扩大为所有 PRD 的固定要求；
5. 用户可见 PRD 使用清楚的业务语言，不输出内部方法名或维护过程。

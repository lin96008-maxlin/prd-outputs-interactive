name: 持续集成检查

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: 运行 Plugin 回归
        run: npm test --prefix plugins/prd-outputs-interactive
